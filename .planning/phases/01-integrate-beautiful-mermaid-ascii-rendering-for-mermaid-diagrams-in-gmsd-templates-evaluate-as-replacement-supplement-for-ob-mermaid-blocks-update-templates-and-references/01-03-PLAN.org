:PROPERTIES:
:phase: 01-integrate-beautiful-mermaid
:plan: 03
:type: execute
:wave: 2
:depends_on: [01-01]
:files_modified: [get-my-shit-done/references/org-styleguide.md, get-my-shit-done/references/diataxis.org, get-my-shit-done/references/diataxis-audit.org]
:autonomous: false
:requirements: [MERMAID-REFS]
:must_haves.truths: ["org-styleguide.md documents MermaidJS ASCII convention instead of ob-mermaid", "Styleguide includes 1-2 rendered ASCII diagram examples", "diataxis.org and diataxis-audit.org ob-mermaid blocks replaced with ASCII", "All source and installed copies are in sync", "No ob-mermaid references remain anywhere in the project"]
:must_haves.artifacts: [{path: get-my-shit-done/references/org-styleguide.md, provides: Updated diagram convention, contains: BEGIN_EXAMPLE}, {path: get-my-shit-done/references/diataxis.org, provides: Diataxis tree as ASCII, contains: BEGIN_EXAMPLE}, {path: get-my-shit-done/references/diataxis-audit.org, provides: Distribution chart as ASCII, contains: BEGIN_EXAMPLE}]
:must_haves.key_links: [{from: "~/.claude/get-my-shit-done/ (installed copies)", to: "get-my-shit-done/ (source)", via: "file copy", pattern: "identical content"}, {from: "org-styleguide.md diagram rule", to: "bin/render-mermaid.cjs", via: "documents usage of render script", pattern: "render-mermaid"}]
:END:

#+title: Plan 03: Update references, styleguides, and sync installed copies
#+author: GMSD
#+date: [2026-03-03]
#+startup: indent
#+options: toc:2 num:nil ^:{}

<objective>
Update all reference and styleguide files to replace ob-mermaid conventions with
MermaidJS ASCII conventions, replace remaining ob-mermaid diagram blocks in reference
files, and synchronize all changes to the installed copies at ~/.claude/get-my-shit-done/.

Purpose: Completes the atomic switch -- after this plan, zero ob-mermaid references exist
anywhere in the project (source or installed). Agents reading the styleguide will produce
MermaidJS ASCII diagrams instead of ob-mermaid blocks.

Output: Updated references, synced installed copies, clean ob-mermaid grep.
</objective>

<execution_context>
@/Users/xavierbrinon/.claude/get-my-shit-done/workflows/execute-plan.md
@/Users/xavierbrinon/.claude/get-my-shit-done/templates/summary.org
</execution_context>

<context>
@.planning/PROJECT.org
@.planning/ROADMAP.org
@.planning/STATE.org
@.planning/phases/01-integrate-beautiful-mermaid-ascii-rendering-for-mermaid-diagrams-in-gmsd-templates-evaluate-as-replacement-supplement-for-ob-mermaid-blocks-update-templates-and-references/01-01-SUMMARY.org
@.planning/phases/01-integrate-beautiful-mermaid-ascii-rendering-for-mermaid-diagrams-in-gmsd-templates-evaluate-as-replacement-supplement-for-ob-mermaid-blocks-update-templates-and-references/01-RESEARCH.org

@get-my-shit-done/references/org-styleguide.md
@get-my-shit-done/references/diataxis.org
@get-my-shit-done/references/diataxis-audit.org
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update org-styleguide.md and replace reference file diagrams</name>
  <files>get-my-shit-done/references/org-styleguide.md, get-my-shit-done/references/diataxis.org, get-my-shit-done/references/diataxis-audit.org</files>
  <action>
1. Update org-styleguide.md "Source Code Blocks" section (lines 116-132):
   - Replace line 121 ob-mermaid rule with MermaidJS ASCII convention:
     "Use #+BEGIN_EXAMPLE / #+END_EXAMPLE for rendered MermaidJS ASCII diagrams
     (produced by bin/render-mermaid.cjs). Preserve Mermaid source in a
     #+begin_src mermaid :exports none block immediately below for re-rendering."
   - Replace the code example (lines 124-132) with a MermaidJS ASCII example
     showing the EXAMPLE + :exports none pattern. Include a small rendered
     ASCII diagram (e.g., a 2-node flowchart) as the example content.
   - This is one of the 1-2 rendered ASCII diagram examples required by the
     user's decision.

2. Replace ob-mermaid block in diataxis.org (~line 362):
   - Extract the Mermaid source (diataxis tree diagram)
   - Render with bin/render-mermaid.cjs
   - Replace with EXAMPLE + :exports none structure
   - Simplify if exceeding 120 columns

3. Replace ob-mermaid block in diataxis-audit.org (~line 127):
   - Extract the Mermaid source (distribution chart)
   - This is an XY chart type -- check if beautiful-mermaid supports it
   - If supported: render and replace as usual
   - If NOT supported or renders poorly: simplify to a text-based table or
     simple ASCII representation that conveys the same distribution data.
     Preserve the original Mermaid source in :exports none block regardless.
  </action>
  <verify>
    <automated>! grep -r "ob-mermaid" get-my-shit-done/references/ && ! grep ":file.*\.png.*:exports results" get-my-shit-done/references/</automated>
  </verify>
  <done>org-styleguide.md documents MermaidJS ASCII convention with example. diataxis.org and diataxis-audit.org diagrams replaced. No ob-mermaid or :file .png references remain in references/.</done>
</task>

<task type="auto">
  <name>Task 2: Sync all changes to installed copies</name>
  <files>~/.claude/get-my-shit-done/templates/, ~/.claude/get-my-shit-done/references/</files>
  <action>
Copy all modified files from source to installed location. This covers both
Plan 02 template changes and Plan 03 reference changes.

1. Copy updated template files (from Plan 02):
   cp get-my-shit-done/templates/adr.org ~/.claude/get-my-shit-done/templates/adr.org
   cp get-my-shit-done/templates/roadmap.org ~/.claude/get-my-shit-done/templates/roadmap.org
   cp get-my-shit-done/templates/research.org ~/.claude/get-my-shit-done/templates/research.org
   cp get-my-shit-done/templates/codebase/architecture.org ~/.claude/get-my-shit-done/templates/codebase/architecture.org
   cp get-my-shit-done/templates/research-project/ARCHITECTURE.org ~/.claude/get-my-shit-done/templates/research-project/ARCHITECTURE.org

2. Copy updated reference files (from this plan):
   cp get-my-shit-done/references/org-styleguide.md ~/.claude/get-my-shit-done/references/org-styleguide.md
   cp get-my-shit-done/references/diataxis.org ~/.claude/get-my-shit-done/references/diataxis.org
   cp get-my-shit-done/references/diataxis-audit.org ~/.claude/get-my-shit-done/references/diataxis-audit.org

3. Copy render script to installed location:
   cp get-my-shit-done/bin/render-mermaid.cjs ~/.claude/get-my-shit-done/bin/render-mermaid.cjs

4. Verify no ob-mermaid references remain in installed copies:
   grep -r "ob-mermaid\|:file.*\.png.*:exports results" ~/.claude/get-my-shit-done/templates/ ~/.claude/get-my-shit-done/references/
   (should return 0 results)
  </action>
  <verify>
    <automated>! grep -r "ob-mermaid\|begin_src mermaid :file" ~/.claude/get-my-shit-done/templates/ ~/.claude/get-my-shit-done/references/ 2>/dev/null && diff get-my-shit-done/references/org-styleguide.md ~/.claude/get-my-shit-done/references/org-styleguide.md</automated>
  </verify>
  <done>All 8 modified files synced to ~/.claude/get-my-shit-done/. Installed copies match source. Zero ob-mermaid references remain in either location.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify ASCII diagrams are readable</name>
  <what-built>All ob-mermaid blocks replaced with MermaidJS ASCII renders across templates
and references. Source and installed copies synced. Styleguide updated.</what-built>
  <how-to-verify>
1. Open get-my-shit-done/templates/adr.org in Emacs -- confirm 2 ASCII diagrams are
   visible and readable without rendering
2. Open get-my-shit-done/templates/research-project/ARCHITECTURE.org -- confirm all 3
   diagram types (flowchart, sequence, state) render readably
3. Open get-my-shit-done/references/org-styleguide.md -- confirm the new MermaidJS ASCII
   convention and example look correct
4. Run: grep -rn "ob-mermaid\|:file.*\.png.*:exports results" get-my-shit-done/
   Confirm 0 results (clean break complete)
5. Run: node --test tests/*.test.cjs
   Confirm all tests pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with specific diagrams</resume-signal>
</task>

</tasks>

<verification>
- grep -r "ob-mermaid" get-my-shit-done/ returns 0 results
- grep -r "begin_src mermaid :file" get-my-shit-done/ returns 0 results
- grep -r "ob-mermaid" ~/.claude/get-my-shit-done/ returns 0 results
- grep -r "BEGIN_EXAMPLE" get-my-shit-done/templates/ returns 9+ results
- diff between source and installed copies shows no differences for modified files
- node --test tests/*.test.cjs passes with 0 failures
</verification>

<success_criteria>
- org-styleguide.md documents MermaidJS ASCII with rendered example
- diataxis.org and diataxis-audit.org diagrams replaced
- All source files synced to ~/.claude/get-my-shit-done/
- Zero ob-mermaid references anywhere in project or installed copies
- Human approves diagram readability
</success_criteria>

<output>
After completion, create .planning/phases/01-integrate-beautiful-mermaid-ascii-rendering-for-mermaid-diagrams-in-gmsd-templates-evaluate-as-replacement-supplement-for-ob-mermaid-blocks-update-templates-and-references/01-03-SUMMARY.org
</output>
