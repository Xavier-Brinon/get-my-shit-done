:PROPERTIES:
:phase: 01-integrate-beautiful-mermaid
:plan: 02
:type: execute
:wave: 2
:depends_on: [01-01]
:files_modified: [get-my-shit-done/templates/adr.org, get-my-shit-done/templates/roadmap.org, get-my-shit-done/templates/research.org, get-my-shit-done/templates/codebase/architecture.org, get-my-shit-done/templates/research-project/ARCHITECTURE.org]
:autonomous: true
:requirements: [MERMAID-TEMPLATES]
:must_haves.truths: ["All 9 ob-mermaid blocks in 5 template files are replaced with rendered ASCII in EXAMPLE blocks", "Each ASCII block has Mermaid source preserved in a :exports none src block below it", "All ASCII diagram lines are <= 120 columns wide", "No ob-mermaid syntax remains in any template file"]
:must_haves.artifacts: [{path: get-my-shit-done/templates/adr.org, provides: ADR template with 2 ASCII diagrams, contains: BEGIN_EXAMPLE}, {path: get-my-shit-done/templates/roadmap.org, provides: Roadmap template with 1 ASCII diagram, contains: BEGIN_EXAMPLE}, {path: get-my-shit-done/templates/research.org, provides: Research template with 1 ASCII diagram, contains: BEGIN_EXAMPLE}, {path: get-my-shit-done/templates/codebase/architecture.org, provides: Codebase architecture template with 2 ASCII diagrams, contains: BEGIN_EXAMPLE}, {path: get-my-shit-done/templates/research-project/ARCHITECTURE.org, provides: Research project architecture with 3 ASCII diagrams, contains: BEGIN_EXAMPLE}]
:must_haves.key_links: [{from: "all template files", to: "bin/render-mermaid.cjs", via: "ASCII output produced by render script", pattern: "BEGIN_EXAMPLE"}, {from: "each ASCII block", to: "Mermaid source block", via: "source preserved below for re-rendering", pattern: "begin_src mermaid :exports none"}]
:END:

#+title: Plan 02: Replace ob-mermaid blocks in all template files
#+author: GMSD
#+date: [2026-03-03]
#+startup: indent
#+options: toc:2 num:nil ^:{}

<objective>
Replace all 9 ob-mermaid blocks across 5 template files with pre-rendered MermaidJS
ASCII diagrams, preserving Mermaid source for future re-rendering.

Purpose: Templates are the primary consumer of diagrams. After this plan, every template
diagram is immediately readable in org-mode buffers and terminals without external
rendering tools.

Output: 5 updated template files with ASCII diagrams in EXAMPLE blocks.

Architectural note: This implements the **Pre-rendered Static Content** pattern -- diagrams
are rendered at authoring time, not at runtime. The render script from Plan 01 is the
authoring tool; the output is committed as static content in templates.
</objective>

<execution_context>
@/Users/xavierbrinon/.claude/get-my-shit-done/workflows/execute-plan.md
@/Users/xavierbrinon/.claude/get-my-shit-done/templates/summary.org
</execution_context>

<context>
@.planning/PROJECT.org
@.planning/ROADMAP.org
@.planning/STATE.org
@.planning/phases/01-integrate-beautiful-mermaid-ascii-rendering-for-mermaid-diagrams-in-gmsd-templates-evaluate-as-replacement-supplement-for-ob-mermaid-blocks-update-templates-and-references/01-01-SUMMARY.org
@.planning/phases/01-integrate-beautiful-mermaid-ascii-rendering-for-mermaid-diagrams-in-gmsd-templates-evaluate-as-replacement-supplement-for-ob-mermaid-blocks-update-templates-and-references/01-RESEARCH.org

@get-my-shit-done/templates/adr.org
@get-my-shit-done/templates/roadmap.org
@get-my-shit-done/templates/research.org
@get-my-shit-done/templates/codebase/architecture.org
@get-my-shit-done/templates/research-project/ARCHITECTURE.org
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ob-mermaid blocks in adr.org, roadmap.org, and research.org</name>
  <files>get-my-shit-done/templates/adr.org, get-my-shit-done/templates/roadmap.org, get-my-shit-done/templates/research.org</files>
  <action>
For each ob-mermaid block in these 3 files (4 blocks total: 2 in adr.org, 1 in roadmap.org, 1 in research.org):

1. Extract the Mermaid source from the existing #+begin_src mermaid :file X.png :exports results block.

2. Render the Mermaid source using the render script:
   node get-my-shit-done/bin/render-mermaid.cjs (pass source via stdin or temp file)

3. If the output exceeds 120 columns, simplify the diagram source:
   - Reduce paddingX (script handles this automatically)
   - If still too wide, shorten node labels while preserving relationships/edges
   - Re-render until output fits

4. Replace the entire ob-mermaid block with the new structure:
   #+NAME: [keep existing name if present]
   #+CAPTION: [keep existing caption, add "(MermaidJS ASCII)" suffix]
   #+BEGIN_EXAMPLE
   [rendered ASCII output]
   #+END_EXAMPLE

   #+begin_src mermaid :exports none
   [original Mermaid source, cleaned of :file and :exports results params]
   #+end_src

5. Remove any #+RESULTS: blocks that ob-mermaid may have left below the original src block.

Blocks to replace:
- adr.org line ~114: flowchart TD (options comparison)
- adr.org line ~129: flowchart LR (impact diagram)
- roadmap.org line ~105: flowchart LR (phase dependencies)
- research.org line ~104: flowchart TD (architecture diagram)
  </action>
  <verify>
    <automated>grep -c "BEGIN_EXAMPLE" get-my-shit-done/templates/adr.org get-my-shit-done/templates/roadmap.org get-my-shit-done/templates/research.org && ! grep ":file.*\.png" get-my-shit-done/templates/adr.org get-my-shit-done/templates/roadmap.org get-my-shit-done/templates/research.org</automated>
  </verify>
  <done>adr.org has 2 EXAMPLE blocks, roadmap.org has 1, research.org has 1. No :file .png references remain. Each EXAMPLE block has a :exports none mermaid source block below it.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ob-mermaid blocks in architecture templates</name>
  <files>get-my-shit-done/templates/codebase/architecture.org, get-my-shit-done/templates/research-project/ARCHITECTURE.org</files>
  <action>
Same replacement procedure as Task 1, applied to the 2 architecture template files
(5 blocks total: 2 in codebase/architecture.org, 3 in research-project/ARCHITECTURE.org):

1. Extract Mermaid source from each ob-mermaid block.
2. Render with render-mermaid.cjs. Simplify if exceeding 120 columns.
3. Replace with EXAMPLE + :exports none source block structure.
4. Remove any #+RESULTS: blocks.

Blocks to replace:
- codebase/architecture.org line ~231: flowchart TD (architecture layers)
- codebase/architecture.org line ~252: sequenceDiagram (architecture flow)
- research-project/ARCHITECTURE.org line ~111: flowchart TD (system overview)
- research-project/ARCHITECTURE.org line ~132: sequenceDiagram (request flow)
- research-project/ARCHITECTURE.org line ~161: stateDiagram-v2 (state flow)

For sequenceDiagram blocks: these may render wider than flowcharts due to participant
labels and message text. Simplify participant aliases and message text if needed to
stay within 120 columns. Preserve the interaction structure (who calls whom).

For stateDiagram-v2: this is the least common type. If it renders poorly, simplify
state names while preserving transitions.
  </action>
  <verify>
    <automated>grep -c "BEGIN_EXAMPLE" get-my-shit-done/templates/codebase/architecture.org get-my-shit-done/templates/research-project/ARCHITECTURE.org && ! grep ":file.*\.png" get-my-shit-done/templates/codebase/architecture.org get-my-shit-done/templates/research-project/ARCHITECTURE.org</automated>
  </verify>
  <done>codebase/architecture.org has 2 EXAMPLE blocks, research-project/ARCHITECTURE.org has 3 EXAMPLE blocks. No :file .png references remain. Each EXAMPLE block has a :exports none mermaid source block below it.</done>
</task>

</tasks>

<verification>
- grep -r "begin_src mermaid :file" get-my-shit-done/templates/ returns 0 results
- grep -r "BEGIN_EXAMPLE" get-my-shit-done/templates/ returns 9 results (matching 9 replaced blocks)
- grep -r ":exports none" get-my-shit-done/templates/ returns 9 results (matching 9 source blocks)
- All ASCII diagrams visually readable at 120 columns
- node --test tests/*.test.cjs still passes (no regressions)
</verification>

<success_criteria>
- All 9 ob-mermaid blocks replaced across 5 template files
- Every diagram has both ASCII render (EXAMPLE) and preserved source (:exports none)
- No :file .png references remain in templates
- All ASCII output lines <= 120 columns
</success_criteria>

<output>
After completion, create .planning/phases/01-integrate-beautiful-mermaid-ascii-rendering-for-mermaid-diagrams-in-gmsd-templates-evaluate-as-replacement-supplement-for-ob-mermaid-blocks-update-templates-and-references/01-02-SUMMARY.org
</output>
